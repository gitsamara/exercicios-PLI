const exibirMensagemArrow = () => {
    console.log("Olá, esta é uma mensagem!");
};

exibirMensagemArrow();

const calcularMediaTresNumerosArrow = (num1, num2, num3) => {
    const media = (num1 + num2 + num3) / 3;
    console.log(`A média é ${media}`);
};

calcularMediaTresNumerosArrow(4, 8, 12);

const quadradoArrow = (num) => num * num;

console.log(quadradoArrow(4)); 
